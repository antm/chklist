%h1 Create a Task

= form_for @task do |t|
  - if @task.errors.any?
    %div.errors
      %h2 #{pluralize @task.errors.count, 'Error'}
      - @task.errors.full_messages.each do |msg|
        %ul
          %li 
            = msg
  = t.text_field :title, placeholder: 'Task title'
  = t.text_field :starttime, placeholder: 'Length'
  = t.check_box :urgent
  = t.submit 
